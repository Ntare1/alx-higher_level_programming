The "my_list" module
====================

using "MyList"
--------------

Importing class from module
    >>> MyList = __import__('1-my_list').MyList

Regular tests
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> print(my_list)
    [1, 4, 2, 3, 5]
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]
    >>> print(my_list)
    [1, 4, 2, 3, 5]

Passing undefined object
    >>> MyList = __import__('1-my_list').MyList
    >>> MyLists.print_sorted()
    Traceback (most recent call last):
             ...
    NameError: name 'MyLists' is not defined

Passing only on integer
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.print_sorted()
    [1]
    >>> print(my_list)
    [1]

Passing child attribute using parent class
    >>> my_list = [3, 6, 9]
    >>> my_list.print_sorted()
    Traceback (most recent call last):
             ...
    AttributeError: 'list' object has no attribute 'print_sorted'

Passing a list
    >>> x = [6, 20, 8]
    >>> my_list = MyList(x)
    >>> my_list.print_sorted()
    [6, 8, 20]

Passing empty list
    >>> x = []
    >>> my_list = MyList(x)
    >>> my_list.print_sorted()
    []
